<!DOCTYPE html>
<html>
    <head>
        @model List<MusicalTasteModel>
        @{
            ViewData["Title"] = "Listagem de Gosto Musical";
        }
    </head>
    <body>
        <div class="text-center">
            <div class="d-grid gap-2 d-md-flex justify-content-md-start mb-1">
                <a class="btn btn-primary" role="button" asp-controller="MusicalTaste" asp-action="Create">Adicionar novo emusicalTastee</a>
            </div>

            @if (TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-success" role="alert">
                    <button type="button" class="btn btn-success btn-sm close-alert" arial-label="Close">X</button>
                    @TempData["SuccessMessage"]
                </div>
            }

            @if (TempData["ErrorMessage"] != null)
            {
                <div class="alert alert-danger" role="alert">
                    <button type="button" class="btn btn-danger btn-sm close-alert" arial-label="Close">X</button>
                    @TempData["ErrorMessage"]
                </div>
            }

            <br />
            <h1 class="display-4">Listagem de Gosto Musical</h1>

            <table class="table" id="musicalTaste-table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Descrição</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (MusicalTasteModel musicalTaste in Model)
                        {
                            <tr>
                                <th scope="row">@musicalTaste.Id</th>
                                <td>@musicalTaste.Description</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a role="button" class="btn btn-primary" asp-route-id="@musicalTaste.Id" asp-controller="MusicalTaste" asp-action="Update">Editar</a>
                                        <a role="button" class="btn btn-danger" asp-route-id="@musicalTaste.Id" asp-controller="MusicalTaste" asp-action="DeleteConfirmation">Excluir</a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr class="odd">
                            <th></th>
                            <td>Nenhum gosto musical cadastrado</td>
                            <td></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </body>
</html>